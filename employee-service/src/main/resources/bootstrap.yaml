spring:
  application:
    name: employee-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/mueblesstgo-employee
    username: root
    password: 93304596
  jpa:
    hibernate:
      ddl-auto: update
